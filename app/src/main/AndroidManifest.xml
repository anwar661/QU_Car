<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.graduation_project">

    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Passenger_Rating"></activity>
        <activity android:name=".Passenger_information" />
        <activity android:name=".Driver_Rating" />
        <activity android:name=".Driver_information" />
        <activity android:name=".Create_trip" />
        <activity android:name=".Book_trip" />
        <activity android:name=".Trips_Available" />
        <activity android:name=".Requests" />
        <activity android:name=".Splash_Screen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Register" />
        <activity android:name=".Login" />
    </application>

</manifest>